security:
    encoders:
        App\Entity\Employe:
            algorithm: pbkdf2

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\Employe
                property: loginEmp

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern: ^/
            form_login:
                check_path: security-login-check
                login_path: security-login
            logout:
                path: security-logout
                target: home
                invalidate_session: false
            anonymous: ~

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
    # - { path: ^/admin, roles: ROLE_ADMIN }
    # - { path: ^/profile, roles: ROLE_USER }

    role_hierarchy:
        ROLE_ADMIN: [ROLE_USER, ROLE_HOTLINER, ROLE_CHAUFFEUR, ROLE_GARAGISTE, ROLE_COMPTABLE, ROLE_RH]
        ROLE_HOTLINER: ROLE_USER
        ROLE_CHAUFFEUR: ROLE_USER
        ROLE_GARAGISTE: ROLE_USER
        ROLE_COMPTABLE: ROLE_USER
        ROLE_RH: ROLE_USER